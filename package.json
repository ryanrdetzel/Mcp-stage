{
  "name": "mcp-stage",
  "version": "0.1.0",
  "description": "The stateful staging environment for MCP servers — record, replay, and fork.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "mcp-stage": "./dist/cli/index.js" },
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@10.0.0"
}
